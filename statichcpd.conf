#Do not modify default values
[DEFAULT]
#dhcp_db_filename = "/etc/statichcpd/Static_DHCP_DB.db"
dhcp_db_filename = Static_DHCP_DB.db
served_interface_regex = ^v.*0$
default_lease_time = 1200
max_lease_time = 100
additional_val_attributes_filename = None

#In the section below, fill any value that needs to be different from default
[statichcpd]
                     
